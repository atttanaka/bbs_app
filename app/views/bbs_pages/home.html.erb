<div class="center jumbotron">
  <h1>
	<%= link_to image_tag("sample.png", alt: "BBS", class: "grow"),
            '/' %>
</h1>

  <h2>
    This is the BBS page
      <h3>
        <%= link_to "escape", "http://google.com/", class: "btn btn-sm btn-danger" %>
      <h3>
  </h2>
</div>
<p>
<%= link_to "POST", "bbsposts/new", class: "btn btn-sm btn-info btn-block" %>

<ul class="bbsposts">
	
	<%= page_entries_info @bbspost %></br>
	<%= will_paginate @bbspost %>
  <% @bbspost.each do |bbspost| %>
<li>
<%= bbspost.title %>
</li>
		<%= bbspost.content %>
(
		<%= bbspost.created_at.strftime("%Y %m/%d %H:%M:%S") %>
:
		<%= bbspost.username %>
)
  <% end %>
</br>
<%= will_paginate @bbspost %>
</ul>
</p>
